const Config = {
    // whether open console.log
    OPEN_LOG: false,
    // 开启超时重发
    OPEN_RESNET_MODE: false,
    // 超时重发的次数
    RESENT_COUNT : 2,
    // 读值指令超时的设定
    COMMAND_SEND_TIMEOUT : 3000,
};

export default Config;